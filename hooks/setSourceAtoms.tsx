import { atom } from "recoil";

export const htmlSource = atom({ key: "html", default: "" })
export const cssSource = atom({ key: "css", default: "" })
export const jsSource = atom({ key: "js", default: "" })
